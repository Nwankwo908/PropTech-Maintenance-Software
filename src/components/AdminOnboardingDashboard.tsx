import { OnboardingWizardShell } from '@/components/onboarding/OnboardingWizardShell'

/** Admin route entry — thin wrapper around the onboarding wizard shell. */
export function AdminOnboardingDashboard() {
  return <OnboardingWizardShell />
}
