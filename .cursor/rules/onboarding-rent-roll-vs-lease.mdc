---
description: Rent roll is portfolio source of truth when present; lease-only uploads may create the portfolio.
alwaysApply: false
---

# Onboarding extraction — rent roll vs lease

## Source of truth

| Document | Creates Properties / Units / Residents | Lease Information Found |
|---|---|---|
| **Rent roll** (or resident roster) | **Yes** — portfolio SoT | No (ignore invented `leases[]`) |
| **Lease agreement** — **no rent roll in the batch** | **Yes** — leases are the portfolio SoT | Yes |
| **Lease agreement** — **rent roll is present** | **Never** from the lease | Yes — enrich matched rent-roll residents |
| **Unknown** type | **Never** — flag for review | No |

## Matching (rent roll present)

Before applying lease fields, match the lease tenant to a rent-roll resident using property + unit + name (and phone/email when present). Formatting differences (`Unit 2A` / `2A`, `John Smith` / `John A. Smith`) are OK when unit/property evidence agrees.

- **Match** → merge lease dates / missing rent onto the same resident (no duplicate).
- **No match** → keep lease in Lease Information (needs review); **do not** mint a resident or unit from that lease.
- **Rent conflict** → keep rent-roll amount; surface `rent_amount_conflict` for review.

## Lease-only uploads

When the landlord uploads only lease documents (no rent roll / roster), use those agreements to fill Properties, Units, Residents, and Lease Information so Fast Track can continue.

## Processing order

Classify each file → extract rent rolls → build portfolio → extract leases → match/enrich (or mint when lease-only) → flag conflicts & unmatched (only when a rent roll is SoT).

## Where it lives

`src/lib/onboardingDocumentUpload.ts` — `classifyOnboardingDocumentExtractRole`, `mergeExtractedDocuments`, `fillExtractedResidentsFromLeases`

## Do not

- Create a resident, unit, or property from a lease **when a rent roll is in the same batch** (except enriching a matched rent-roll resident)
- Treat the whole upload batch as one document type
- Silently overwrite rent-roll rent with a conflicting lease amount
